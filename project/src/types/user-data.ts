export type UserData = {
  id: number;
  email: string;
  token: string;
  avatarUrl: string;
  name: string;
  isPro?: boolean;
};

export type UserType = {
  id: number;
  email: string;
  avatarUrl: string;
  name: string;
  isPro?: boolean;
}
